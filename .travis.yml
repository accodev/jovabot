language: python
python:
  - "3.4"
  - "3.5"
install:
  - pip install coveralls
  - pip install -r reqs/test-requirements.txt
script: "py.test --cov=modules tests/"
after_success:
  coveralls
env:
  - USE_DICT_DB=1
